# 我思故我在 · 阅读器 — 功能清单

> 文档用途：项目功能回溯与迭代参考  
> 最后更新：2026-06-13（对应提交 `bb6c31f` 及同批次本地改动）  
> 仓库：`https://github.com/lulu99999/ebook.git` · 分支 `master`

---

## 1. 项目概述

| 项 | 说明 |
|---|---|
| 名称 | 我思故我在 · 智能艺术阅读空间 |
| 包名 | `cognito-ebook-reader` |
| 形态 | 单页 Web 应用（主逻辑集中在 `code_artifact.html`） |
| 定位 | 本地优先的电子书阅读与读书笔记工具，支持 PWA 离线安装 |
| 启动 | `npm start` → `http://localhost:3000`（`serve` 端口 3000） |
| 入口 | `index.html` 自动跳转至 `code_artifact.html` |

---

## 2. 技术架构

### 2.1 文件结构

| 路径 | 作用 |
|---|---|
| `code_artifact.html` | 主应用：UI、业务逻辑、样式 |
| `index.html` | 入口重定向 |
| `manifest.webmanifest` | PWA 清单（standalone、图标、主题色） |
| `sw.js` | Service Worker（缓存 v13：`cognito-reader-v13`） |
| `vendor/` | 离线依赖：Tailwind、Lucide、PDF.js、JSZip、MOBI 解析、EPUB 目录提取 |
| `vendor/extract-epub-toc.js` | 基于 foliate-js 提取 EPUB 目录（含层级 depth） |
| `icons/` | PWA 图标（192/512、透明底图等） |
| `scripts/make-transparent-icon.py` | 图标去白底辅助脚本 |
| `node_modules/foliate-js` | Kindle/MOBI 解析引擎（npm 依赖） |

### 2.2 前端依赖

- **Tailwind CSS**（本地 CDN 包）
- **Lucide** 图标
- **PDF.js** — PDF 文本提取
- **JSZip** — EPUB 解压解析
- **foliate-js / mobi.js** — MOBI / AZW / AZW3 / PRC 解析
- **Web Speech API** — 听书朗读
- **IndexedDB + localStorage** — 本地持久化

### 2.3 应用标识

- `appId` 默认：`modern-cognito-reader`
- IndexedDB 库名：`{appId}_library_v1`

---

## 3. 功能清单

### 3.1 启动与全局

| 功能 | 说明 |
|---|---|
| 启动闪屏 | 全屏闪屏 + Logo，点击「进入阅读空间」后初始化 |
| 深色模式 | 顶栏一键切换 `dark` class，影响书架/统计等全局 UI |
| Toast 提示 | 操作反馈（导入、导出、删除、主题切换等） |
| 离线可用 | vendor 资源本地化；SW 预缓存核心静态文件 |
| iPhone / 移动端适配 | `100dvh`、`viewport-fit=cover`、safe-area、`tab-scroll-pad` |
| PWA meta | `mobile-web-app-capable` + `apple-mobile-web-app-capable` |
| 控制台降噪 | 过滤 Tailwind CDN 生产环境警告（不影响功能） |
| 禁用缩放 | `user-scalable=no`，阅读体验更稳定 |

---

### 3.2 主导航（底部三 Tab）

| Tab | ID | 名称 | 功能概要 |
|---|---|---|---|
| 书屋 | `tab-bookshelf` | 书架首页 | 续读卡片、每日进度、图书网格 |
| 心流 | `tab-stats` | 阅读统计 | 今日/累计时长、成就环、周趋势图 |
| 印记 | `tab-history` | 阅读足迹 | 按书记录的阅读历史条目 |

---

### 3.3 书架（书屋）

| 功能 | 说明 |
|---|---|
| 续读卡片 | 显示最近阅读书籍、进度条、「继续阅读」按钮 |
| 每日阅读进度 | 展示「已阅读 X / 30 分钟」，可跳转心流 Tab |
| 拟物化书脊封面 | 根据书名哈希生成渐变封面；网格展示 |
| 点击开书 | 打开阅读器，恢复上次段落位置 |
| 内置样例书 | 首次无本地数据时注入 2 本哲学样例（笛卡尔、苏格拉底） |
| 书架恢复横幅 | 检测到备份比当前书架更完整时提示；可手动「尝试恢复书架」 |
| 删书交互 | **长按约 0.5s** 或 **右键** 显示删除钮；确认后删除书及关联数据 |
| 删书后备份同步 | `syncLibraryBackupsToCurrent()`，避免误弹恢复横幅 |

**明确不包含：**

- 书架大卡片导入入口（已移除，勿恢复）
- 书架 JSON 备份导入/导出（已移除，勿恢复）

---

### 3.4 图书导入

| 功能 | 说明 |
|---|---|
| 顶栏「导入书籍」按钮 | `triggerBookImport()` → 文件选择器 |
| 拖拽导入 | 拖入书屋区域即可导入（高亮提示） |
| 支持格式 | `.txt` `.pdf` `.epub` `.mobi` `.azw` `.azw3` `.prc` |
| 不支持 | KFX（提示用 Calibre 转换）；DRM 加密书籍 |
| TXT | UTF-8 直接读取 |
| PDF | PDF.js 逐页提取文本，按页分段 |
| EPUB | JSZip 解压 + spine 顺序解析；Foliate 优先提取 nav 目录，回退自定义 nav/NCX 解析 |
| EPUB 目录 | 支持二级（多级）目录缩进展示；`href` 与 spine 完整路径匹配，避免同名文件错挂 |
| EPUB 章节标记 | 导入时写入 `第[章节]` / `第[章节·N]` 深度标记，供目录与正文识别 |
| Kindle/MOBI | foliate-js `MOBI` 解析，提取标题/作者/正文 |
| 导入反馈 | Toast 显示解析进度与结果 |

---

### 3.5 阅读器

#### 3.5.1 沉浸式全屏

| 功能 | 说明 |
|---|---|
| 全屏覆盖 | `#reader-view` 固定全屏，`z-50` |
| 默认隐藏 Chrome | `reader-chrome-hidden`：顶栏上移隐藏、底栏透明融入背景 |
| 点击切换 | 点击阅读区 `toggleReaderChrome()` 显示/隐藏顶栏 |
| 顶栏/底栏定位 | 底栏改为 flex 占位（`shrink-0`），不覆盖正文；顶栏仍为 absolute 叠层 |
| 夜间联动 | 全局 dark 模式下开书自动设 `readerConfig.theme = 'midnight'` |

#### 3.5.2 顶栏工具

| 按钮 | 功能 |
|---|---|
| 返回 | 关闭阅读器，保存进度 |
| 读书随笔 | 打开笔记侧栏 |
| AI 听书 | 打开 TTS 控制面板 |
| 排版微调 | 打开排版配置面板 |
| 目录检索 | 打开章节目录侧栏 |

#### 3.5.3 阅读模式

| 模式 | 说明 |
|---|---|
| 滚动模式 `scroll` | 默认；长文分批渲染（`SCROLL_RENDER_CHUNK = 50`）提升性能 |
| 翻页模式 `paged` | 按视口分页排版；异步分批构建布局；横滑交给 `scroll-snap` |
| 章节识别 | `第[章节]` / `第[章节·N]` 或含「章/节」的段落作为目录与样式区分 |

#### 3.5.4 排版与主题

| 配置项 | 选项 / 范围 |
|---|---|
| 阅读主题 | 象牙白、雅致沙、松针绿、静谧灰、深邃蓝、纯臻黑 |
| 字号 | 增减调节（默认 18px） |
| 字体 | 现代等宽（sans）/ 经典衬线（serif） |
| 页边距 | 16–72px 滑块 |
| 底栏主题联动 | `applyReaderChromeTheme()` 使底栏与阅读背景同色 |

#### 3.5.5 进度与导航

| 功能 | 说明 |
|---|---|
| 底栏进度条 | 拖动跳转段落/页；左右按钮翻页/段 |
| 页码展示 | 分页模式显示「第 X / Y 页」；滚动模式显示「第 X / Y 段」；百分比为辅 |
| 新书起始位置 | 未读书籍（进度 0）首次打开从第 1 段/第 1 页开始，不继承上一本书滚动位置 |
| 关闭时保存 | `lastReadPara`、百分比 `progress` 写回书籍 |
| 滑动手势 | 横屏分页：原生横滑 + `scroll-snap`；点击左右区域翻页 |
| 点击翻页 | 与滑动手势去重，避免一次跳两页 |
| 阅读计时 | 打开书开始计时，关闭时累加统计 |

#### 3.5.6 目录

| 功能 | 说明 |
|---|---|
| 自动构建 | 从章节段落生成目录；EPUB 优先 Foliate TOC，含二级目录缩进 |
| 点击跳转 | 跳转到对应段落/页 |
| 侧栏 + 遮罩 | 独立 catalog panel，滚动不与主视口冲突 |

---

### 3.6 笔记与划线

| 功能 | 说明 |
|---|---|
| 文本选择 | 选中正文弹出浮动工具条；支持长段拖选与同段跨分页块选区 |
| 系统菜单屏蔽 | 弹出标注层后收起系统选区/复制菜单，避免遮挡自定义工具条 |
| 标注预览 | `CSS.highlights` 预览选区；全屏透明遮罩 `#selection-marking-backdrop` |
| 五色高亮 | 黄 / 绿 / 粉 / 蓝 / 紫 |
| 记笔记 | 关联选中文本或独立随笔 |
| 编辑/删除标记 | 点击已有高亮可编辑附记或移除 |
| 笔记侧栏 | 按书列出全部笔记，可跳转原文 |
| 独立随笔 | 不绑定段落的自由笔记 |
| 偏移计算 | 分页模式下同一段多块 DOM 时，按全文偏移定位高亮（`getParagraphTextOffset`） |
| 笔记数据结构 | `db.notes[bookId]` 数组，含 `paraIndex`、`text`、`color`、`note` 等 |

---

### 3.7 笔记导出

| 功能 | 说明 |
|---|---|
| 顶栏导出入口 | `openNotesExportPicker()` 打开按书选择面板 |
| 多选导出 | 勾选多本书，分别下载 JSON 文件 |
| 阅读页导出 | 笔记侧栏「导出本书笔记」 |
| 导出格式 | JSON（含书名、作者、笔记条目） |
| 无笔记提示 | 无笔记时 Toast 提示，不弹空面板 |

**明确不包含：**

- 书架整体 JSON 导入/导出（仅保留笔记导出）

---

### 3.8 听书（TTS）

| 功能 | 说明 |
|---|---|
| 引擎 | 浏览器 `speechSynthesis` |
| 语音选择 | 中英文系统语音列表 |
| 语速调节 | `speechSpeed` 滑块 |
| 播放控制 | 播放/暂停、停止、上一段/下一段 |
| 悬浮入口 | 朗读进行中显示 `#tts-fab` 快速打开控制面板 |
| 面板层级 | TTS / 排版面板 `z-index` 高于底栏，不被进度条遮挡 |
| 朗读跟随 | 滚动/翻页模式自动定位当前朗读段落；翻页/跳转后恢复朗读 |
| 段落高亮 | 朗读中段落 `speech-highlight` 样式 |
| 语音推荐 | 按自然度打分排序，★ 标记推荐中文语音 |
| Media Session | 锁屏/耳机控制：播放、暂停、上/下段 |
| 静音音频 | `silent-player` 辅助保持后台播放（移动端） |

---

### 3.9 心流统计（`tab-stats`）

| 指标 | 说明 |
|---|---|
| 今日阅读 | 分钟数 |
| 累计阅读 | 总分钟数 |
| 书架藏书 | 书籍数量 |
| 阅读印记 | 历史记录条数 |
| 今日成就 | 相对 30 分钟目标的百分比环 |
| 周趋势图 | SVG 折线图，周一到周日 |
| 按周重置 | `getWeekId()` 每周一重置 `weekly` 数组 |
| 按日重置 | 跨日重置 `today` 与 `bookToday` |
| 清空记录 | `clearReadingStats()` 一键清零 |

---

### 3.10 阅读印记（`tab-history`）

| 功能 | 说明 |
|---|---|
| 足迹列表 | 每次开书写入历史（书名、时间、段落位置） |
| 点击续读 | 从历史条目重新打开对应书籍 |
| 删书联动 | 删除书籍时同步清除该书历史 |

---

### 3.11 PWA

| 功能 | 说明 |
|---|---|
| 可安装 | `display: standalone`，`start_url: /code_artifact.html` |
| 图标 | 192×192、512×512（含 maskable） |
| 主题色 | `#4f46e5` |
| iOS 支持 | `apple-mobile-web-app-*` meta |
| Service Worker | 安装预缓存；HTML/manifest/icons 网络优先；其余缓存优先 |
| 更新注意 | 修改后需升 `CACHE_NAME` 版本；用户需强刷或重装 PWA |

---

### 3.12 数据存储与恢复

#### 3.12.1 数据模型 `db`

```text
db = {
  books: [],        // 书籍：id, title, author, content, progress, lastReadPara, ...
  history: [],      // 阅读足迹
  notes: {},        // 按 bookId 索引的笔记数组
  stats: {
    today, total, weekly[7], bookToday, lastUpdated, weekId
  }
}
```

另存 `readerConfig`：主题、字号、字体、模式、页边距。

#### 3.12.2 存储策略

| 层级 | 说明 |
|---|---|
| IndexedDB | 主存储，存完整书架快照（大容量） |
| localStorage | 兼容旧版 + `reader_config` + 双份旋转备份（`_db_backup_a/b`） |
| 启动恢复 | 优先 IndexedDB → 备份恢复 → 旧版 localStorage |
| 智能恢复 | `isLikelyIntentionalDeletion()` 避免用户删书后误恢复旧备份 |
| 保存失败回滚 | 删书/导入失败时恢复内存快照并提示 |

---

## 4. 版本演进（Git 提交）

| 提交 | 摘要 |
|---|---|
| `4fda44d` | 初始版本：网页阅读器 |
| `d5ccba7` | PWA、笔记导出、移动端阅读优化 |
| `22b31fb` | 恢复图书导入（顶栏 + 拖拽） |
| `768d2c9` | 精简导入入口、深邃蓝主题、续读卡片圆角修复 |
| `beb34d9` | 沉浸式阅读、长按/右键删书、删书备份同步、dark→midnight |
| `ab86716` | 阅读进度、目录跳转、TTS、启动体验修复 |
| `b3342f1` | 阅读滚动与分页布局回归修复 |
| `0683eaf` | EPUB 真实 TOC 解析、章节标题保留 |
| `14262f9` | TTS 导航后恢复朗读、自然语音排序 |
| `bb6c31f` | 分页阅读/标注/目录/TTS 面板布局等综合优化（见 §8） |

---

## 5. 运维与测试要点

### 5.1 本地开发

```bash
npm install   # 首次
npm start     # http://localhost:3000
```

### 5.2 部署后验证清单

- [ ] PWA 可安装，图标正常
- [ ] 顶栏「导入书籍」可导入 EPUB/PDF/TXT/MOBI
- [ ] 拖拽文件到书屋可导入
- [ ] 阅读器默认隐藏顶栏，点击正文可唤出
- [ ] 横屏分页可横滑翻页，页码为「第 X / Y 页」
- [ ] 新书首次打开从第 1 页/段开始
- [ ] 长段文字可选中并标注，工具条不被系统菜单遮挡
- [ ] 朗读中悬浮耳机按钮可打开 TTS 面板
- [ ] 长按书脊可删书，删后不再弹恢复横幅
- [ ] 顶栏笔记导出：多选分书导出 JSON
- [ ] 阅读页侧栏可导出本书笔记
- [ ] 心流统计按日/周正确重置
- [ ] iPhone 安全区与底部 Tab 不被遮挡
- [ ] 更新 SW 版本后强刷生效

### 5.3 已知限制

- PDF 仅提取可复制文本层，扫描版效果差
- DRM / KFX 不支持
- TTS 依赖系统语音，不同设备体验差异大
- 大书首次分页排版可能短暂显示「正在排版」
- 分页模式下跨**不同段落**的选区仅标注起始段内文字
- 删书不扣减历史累计阅读总时长（设计如此，见删除确认文案）

---

## 6. 迭代注意事项（防回归）

1. **勿移除**顶栏「导入书籍」与拖拽导入逻辑（`processBookFile`、`triggerBookImport`）。
2. **勿恢复**书架大卡片导入入口、书架 JSON 备份导入/导出（用户已明确不要）。
3. 删书流程须保留 `syncLibraryBackupsToCurrent()`，防止恢复横幅误报。
4. 修改静态资源或 `code_artifact.html` 后，记得更新 `sw.js` 中 `CACHE_NAME`。
5. PWA 用户需提示强刷（Ctrl+Shift+R）或删除后重新「添加到主屏幕」。
6. 分页排版修改后须验证：多页横滑正常、新书从第 1 页开始、长段标注偏移正确。

---

## 7. 关键函数索引（便于代码定位）

| 领域 | 函数 |
|---|---|
| 导航 | `switchTab`, `dismissSplash`, `initApp` |
| 导入 | `processBookFile`, `parseEpubBook`, `extractEpubTocWithFoliate`, `setupDragDropImport` |
| 书架 | `renderBookshelf`, `bindBookshelfItem`, `showBookshelfDeleteAction`, `deleteBook` |
| 阅读 | `openReader`, `closeReader`, `renderReaderContent`, `toggleReaderChrome` |
| 分页 | `buildPagedLayoutAsync`, `renderPagedContentAsync`, `restorePagedPosition` |
| 排版 | `changeReadingMode`, `changeReaderTheme`, `applyReaderConfig` |
| 笔记 | `applyHighlight`, `scheduleSelectionCheck`, `getParagraphTextOffset`, `saveNote`, `exportNotes` |
| 听书 | `speakParagraph`, `togglePlaySpeech`, `initTtsVoices`, `openTtsControlPanel` |
| 统计 | `renderStats`, `checkDailyStatsReset`, `getWeekId` |
| 存储 | `saveToLocalStorage`, `loadFromLocalStorage`, `recoverLibraryFromBackups` |

---

## 8. 本次更新摘要（2026-06-13）

> 涵盖 `bb6c31f` 提交及同批次本地改动，便于回溯与验收。

### 8.1 EPUB 与目录

- Foliate 优先提取 EPUB 目录（`vendor/extract-epub-toc.js`），nav/NCX 双回退。
- 书架目录支持**二级目录**缩进展示。
- 修复 EPUB3 `epub:type` 解析、TOC `href` 与 spine 路径匹配（避免 basename 错挂章节）。
- 导入时写入带深度的章节标记 `第[章节·N]`，目录与正文跳转更准确。

### 8.2 阅读器与分页

- 底栏改为 flex 占位，不再遮挡正文；显示**第 X / Y 页**（分页）或**第 X / Y 段**（滚动）。
- **新书首次打开**从第 1 页/段开始，不继承上一本书的滚动位置。
- 横屏分页：异步分批排版、测量沙箱修复（避免整本书被排成一页）。
- 横滑翻页优先原生 `scroll-snap`；修复点击翻页一次跳两页。
- 旋转/resize 时仅在视口尺寸变化时重建分页，减少无效重排。

### 8.3 标注与选区

- 弹出标注工具条时屏蔽系统复制/选区菜单，避免遮挡。
- `selection-marking-backdrop` + `marking-preview` 高亮预览。
- 修复长段拖选：手势进行中不提前弹层打断选区。
- 分页模式下同一段多块 DOM 时，按全文偏移计算高亮位置（`getParagraphTextOffset`）。
- 跨段落选区时裁剪到起始段末尾（不跨段标注）。

### 8.4 听书（TTS）

- 朗读中显示悬浮耳机按钮（`#tts-fab`）快速打开控制面板。
- TTS / 排版配置面板抬升至底栏之上，横屏不被遮挡。
- 翻页、目录跳转后自动恢复朗读位置。

### 8.5 其他

- 新增 `mobile-web-app-capable` meta。
- 过滤 Tailwind CDN 生产警告日志。
- Bugbot 驱动修复：TTS 面板层级、EPUB href 匹配、分页测量与选区边界等。

### 8.6 验收建议

- [ ] 新导入 EPUB：目录含二级项、章节跳转正确
- [ ] 新书上架后首次打开：位于第 1 页/段
- [ ] 横屏分页：多页横滑流畅，页码显示正确
- [ ] 长段拖选标注：松手后出现颜色工具条，高亮位置准确
- [ ] 朗读中：悬浮按钮可开面板，面板不被底栏挡住

---

*本文档随功能变更同步更新；以 `code_artifact.html` 源码为准。*
